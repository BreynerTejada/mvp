import { SxProps, Theme } from '@mui/material';

export const pageContainerSx: SxProps<Theme> = {
  py: 8,
};

export const cardSx: SxProps<Theme> = {
  p: 4,
  mt: 4,
};

export const alertSx: SxProps<Theme> = {
  mb: 3,
};

export const textFieldSx: SxProps<Theme> = {
  input: { color: '#fff' },
};

export const passwordFieldSx: SxProps<Theme> = {
  input: { color: '#fff' },
  mb: 4,
};

export const inputLabelStyle = { color: '#ccc' };
